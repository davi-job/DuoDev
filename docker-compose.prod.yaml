volumes:
    duodev_data:

services:
    backend:
        env_file: .env.prod

    admin-back:
        env_file: .env.prod

    frontend:
        env_file: .env.prod
        ports:
            - 8021:3000

    admin-front:
        env_file: .env.prod
        ports:
            - 8021:3000

    postgres:
        env_file: .env.prod
        volumes:
            - duodev_data:/var/lib/postgresql/
